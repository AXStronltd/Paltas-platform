node_modules
.next
.git
.github
tests
.test-build
*.md
!README.md
# Design references. Both are large HTML exports kept for reference; neither is
# imported, served or built, and PROTOTYPE-reference.html was already excluded
# while its newer sibling — the larger of the two — was not.
PROTOTYPE-reference.html
Paltas_real_state.html
.env
.env.*

# A second application living in this repo. It has its own toolchain and is
# not built or served by this image; copying it in only enlarges the build
# context and the layer cache.
property-management
