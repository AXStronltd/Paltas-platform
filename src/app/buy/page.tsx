import { BuyBrowser } from "@/components/marketplace/BuyBrowser";

export const metadata = {
  title: "Property for sale — PALTAS",
  description: "Homes, apartments and land for sale.",
};

export default function BuyPage() {
  return <BuyBrowser />;
}
