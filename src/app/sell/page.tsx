import { SellForm } from "@/components/marketplace/SellForm";

export const metadata = {
  title: "Sell your property — PALTAS",
  description: "List your property for sale with PALTAS.",
};

export default function SellPage() {
  return <SellForm />;
}
